// dbconn.bucket.upsert(id, {name:username, title:title, date:date, note:note, type:'todo'}, (err2, res2)=>{
            //     console.log(res2)
            // })
            // dbconn.bucket.query(dbconn.N1qlQuery.fromString('SELECT * FROM `TODO` WHERE `TODO`.`name`="'+username+'" AND `TODO`.`type`="todo"'),
            // (err, rows)=>{
            //     res.json({rows})
            // })